%h1 New <%= singular_name.titleize %>

= error_messages_for :<%= singular_name %>

- form_for @<%= singular_name %> do |f|
  = render :partial => "form", :locals => { :f => f }
  %p= submit_tag "Create"
%br
%p= link_to 'Back', collection_path
